const initialState = {
    // Some initial state
}

const rootReducer = (state = initialState, action) => {
    // Reducers should generally have a switch statement that edits a copy of the state according to which action is passed
    return state;
}

export default rootReducer;
